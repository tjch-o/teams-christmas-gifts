datasource db {
    provider =  "sqlite"
    url = "file:./staff.db"
}

generator client {
    provider = "prisma-client-js"
}

model staff {
    staff_pass_id String @id
    team_name String 
    created_at String
}